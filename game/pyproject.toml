[project]
name = "sl4-engine"
version = "0.1.0"
requires-python = ">=3.10"

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B", "C4", "SIM"]
ignore = ["E501", "E402"]

[tool.ruff.format]
quote-style = "double"

[tool.black]
line-length = 100